# Escreva um algoritmo que converta uma temperatura digitada em °C para °F.
temp = float(input("Informe a temperatura em ºC: "))
print(f"Convertendo {temp}°C para fahrenheit, temos {((9 * temp) / 5) + 32}°F")
